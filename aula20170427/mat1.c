#include <stdio.h>
#include <math.h>

main (){
double a, b, c, d, i;
double A, B;
scanf ("%lf %lf %lf %lf", &a, &c, &b, &d);
A=((b-a)*(b-a))+((d-c)*(d-c));
A=sqrt(A)
printf ("%.4lf\n", A);
}
